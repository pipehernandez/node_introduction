const os = require("os");
console.log(os.arch()); // Se obtiene la arquitectura de la CPU.
console.log(os.cpus()); // se obtiene la informacion sobre la CPUs.
console.log(os.freemem()); // se obtiene la cantidad de memoria libre.
console.log(os.hostname()); // se obtiene el nombre del host.